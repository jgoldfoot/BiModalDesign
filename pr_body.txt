This PR addresses terminology drift and cleans up invalid, incorrectly spaced, or stale brand identifiers across the repository to ensure strict alignment with the canonical brand: **BiModal Design**.

### Changed Files

**`docs/api-reference.md`**
- Replace stale `AgentUX` and `agentux` occurrences in imports and paths with `bimodal-design`. **[HIGH]**
- Correct space-separated identifiers to valid CamelCase code (e.g. `BiModal DesignServer` → `BiModalDesignServer`, `useBiModal Design` → `useBiModalDesign`, `BiModal DesignTester` → `BiModalDesignTester`, `BiModal DesignService` → `BiModalDesignService`). **[HIGH]**

**`examples/astro-ssg-example.md`**
- Correct space-separated identifier `window.BiModal Design` to valid JavaScript object path `window.BiModalDesign`. **[HIGH]**

**`examples/react-spa-example.md`**
- Correct space-separated identifier `window.BiModal Design` to valid JavaScript object path `window.BiModalDesign`. **[HIGH]**

### Exclusions & Notes
- Package configuration (`package.json`) was verified and found correct.
- Historical occurrences in the migration script `fix-urls.sh` were preserved to maintain historical context as it pertains to a past domain migration.

All tests pass and formatting has been verified.
