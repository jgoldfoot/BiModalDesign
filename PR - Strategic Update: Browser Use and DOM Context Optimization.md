# Strategic Update: Browser Use and DOM Context Optimization

This PR introduces a strategic update proposal for optimizing the DOM for open-source AI agents like `browser-use`. It addresses the problem of "div soup" and context window bloat, proposing a flattened, semantically dense DOM structure. Included is a new markdown proposal document and an HTML example demonstrating the pattern.
